export default function UrlRules() {
    return (
        <p className="cfv-rules-note">
            URL format is always validated automatically — value must start with
            http:// or https://. No additional rules are needed.
        </p>
    );
}
